import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#F1EFEC",
      main: "#BFBDB8",
    },
    secondary: {
      main: "#F7C113",
    },
  },
});
export default theme;
